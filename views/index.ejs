
  <%- include('./includes/_head.ejs') %>
  <body>
    <%- include('./includes/_navbar.ejs') %>
    <div class="container pt-5">
      <h1 class="display-4"><%= pageTitle %></h1>
      <p class="lead">There are <%= users.length %> users.</p>
      <%- include('./includes/_userModal.ejs') %>
      
      <div>
        <div class="list-group mt-4">
        <% if(users.length > 0) { %>
            <% for(let user of users) { %>
            
              <a href="/users/<%=user._id %>" class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                <%= user.name %>
                <span class="badge badge-primary badge-pill">Total Cars: <%= user.cars.length %></span>
              </a>
          
    
            <% } %>
          <% } else { %>
            <h2>No Users!</h2>
          <% } %>
        </ul>
      </div>
    </div>


    <%- include('./includes/_scripts.ejs') %>
    <script src="/js/app.js"></script>
</html>
